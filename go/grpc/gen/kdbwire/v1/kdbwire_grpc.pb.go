// Code generated by protoc-gen-go-grpc. DO NOT EDIT.
// versions:
// - protoc-gen-go-grpc v1.6.2
// - protoc             v4.25.3
// source: kdbwire/v1/kdbwire.proto

// The KDB wire protocol, carried over HTTP/2.
//
// This is deliberately a transport, not a second protocol. The payload of every Frame is one
// complete, length-prefixed KDB wire frame - byte for byte what the TCP and WebSocket listeners
// carry (see docs/kdb-spec-layer7-component21-wire-protocol-framing.md). Everything that gives
// those frames meaning - the handshake and its authentication, RBAC, sessions, SQL execution -
// is unchanged and shared, so a client that can already encode KDB frames needs no new codec to
// speak this, and there is no second dispatch table to keep in step with the wire message types.

package kdbwirev1

import (
	context "context"
	grpc "google.golang.org/grpc"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
)

// This is a compile-time assertion to ensure that this generated file
// is compatible with the grpc package it is being compiled against.
// Requires gRPC-Go v1.64.0 or later.
const _ = grpc.SupportPackageIsVersion9

const (
	KdbWire_Connect_FullMethodName = "/kdb.wire.v1.KdbWire/Connect"
)

// KdbWireClient is the client API for KdbWire service.
//
// For semantics around ctx use and closing/ending streaming RPCs, please refer to https://pkg.go.dev/google.golang.org/grpc/?tab=doc#ClientConn.NewStream.
type KdbWireClient interface {
	// Connect carries one client connection.
	//
	// One stream is one connection, and that is load-bearing rather than incidental. Sessions are
	// scoped to a connection: they are authenticated once at handshake time and reused for every
	// later frame, they own document locks, and they require that two frames naming the same
	// session are served in the order they were sent. A single bidirectional stream preserves all
	// of that; spreading one client's frames over several concurrent streams would not, because
	// HTTP/2 gives no ordering between streams.
	//
	// When the stream ends, every session opened on it is closed and every document lock it held
	// is released - the same lifecycle a dropped TCP connection gets.
	Connect(ctx context.Context, opts ...grpc.CallOption) (grpc.BidiStreamingClient[Frame, Frame], error)
}

type kdbWireClient struct {
	cc grpc.ClientConnInterface
}

func NewKdbWireClient(cc grpc.ClientConnInterface) KdbWireClient {
	return &kdbWireClient{cc}
}

func (c *kdbWireClient) Connect(ctx context.Context, opts ...grpc.CallOption) (grpc.BidiStreamingClient[Frame, Frame], error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	stream, err := c.cc.NewStream(ctx, &KdbWire_ServiceDesc.Streams[0], KdbWire_Connect_FullMethodName, cOpts...)
	if err != nil {
		return nil, err
	}
	x := &grpc.GenericClientStream[Frame, Frame]{ClientStream: stream}
	return x, nil
}

// This type alias is provided for backwards compatibility with existing code that references the prior non-generic stream type by name.
type KdbWire_ConnectClient = grpc.BidiStreamingClient[Frame, Frame]

// KdbWireServer is the server API for KdbWire service.
// All implementations must embed UnimplementedKdbWireServer
// for forward compatibility.
type KdbWireServer interface {
	// Connect carries one client connection.
	//
	// One stream is one connection, and that is load-bearing rather than incidental. Sessions are
	// scoped to a connection: they are authenticated once at handshake time and reused for every
	// later frame, they own document locks, and they require that two frames naming the same
	// session are served in the order they were sent. A single bidirectional stream preserves all
	// of that; spreading one client's frames over several concurrent streams would not, because
	// HTTP/2 gives no ordering between streams.
	//
	// When the stream ends, every session opened on it is closed and every document lock it held
	// is released - the same lifecycle a dropped TCP connection gets.
	Connect(grpc.BidiStreamingServer[Frame, Frame]) error
	mustEmbedUnimplementedKdbWireServer()
}

// UnimplementedKdbWireServer must be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedKdbWireServer struct{}

func (UnimplementedKdbWireServer) Connect(grpc.BidiStreamingServer[Frame, Frame]) error {
	return status.Error(codes.Unimplemented, "method Connect not implemented")
}
func (UnimplementedKdbWireServer) mustEmbedUnimplementedKdbWireServer() {}
func (UnimplementedKdbWireServer) testEmbeddedByValue()                 {}

// UnsafeKdbWireServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to KdbWireServer will
// result in compilation errors.
type UnsafeKdbWireServer interface {
	mustEmbedUnimplementedKdbWireServer()
}

func RegisterKdbWireServer(s grpc.ServiceRegistrar, srv KdbWireServer) {
	// If the following call panics, it indicates UnimplementedKdbWireServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	s.RegisterService(&KdbWire_ServiceDesc, srv)
}

func _KdbWire_Connect_Handler(srv interface{}, stream grpc.ServerStream) error {
	return srv.(KdbWireServer).Connect(&grpc.GenericServerStream[Frame, Frame]{ServerStream: stream})
}

// This type alias is provided for backwards compatibility with existing code that references the prior non-generic stream type by name.
type KdbWire_ConnectServer = grpc.BidiStreamingServer[Frame, Frame]

// KdbWire_ServiceDesc is the grpc.ServiceDesc for KdbWire service.
// It's only intended for direct use with grpc.RegisterService,
// and not to be introspected or modified (even as a copy)
var KdbWire_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "kdb.wire.v1.KdbWire",
	HandlerType: (*KdbWireServer)(nil),
	Methods:     []grpc.MethodDesc{},
	Streams: []grpc.StreamDesc{
		{
			StreamName:    "Connect",
			Handler:       _KdbWire_Connect_Handler,
			ServerStreams: true,
			ClientStreams: true,
		},
	},
	Metadata: "kdbwire/v1/kdbwire.proto",
}
